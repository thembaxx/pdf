class File {
  constructor(name, size, url, fileType, fileExt) {
    this.name = name;
    this.size = size;
    this.url = url;
    this.fileType = fileType;
    this.fileExt = fileExt;
  }
}

export default File;
